import React from 'react';
import { Text, Box } from '@chakra-ui/react';

const Footer = () => {
  return (
    <Box mt="auto" py="6">
      <Text textAlign="center">
        Todo App using Apollo + React + Chakra UI + Node.js + MongoDB
      </Text>
    </Box>
  );
};

export default Footer;
